all:
  children:
    client:
      hosts:
        jmeter-client:
          ansible_port: 22 
          ansible_host: "{{ vault_jmeter_client_ip }}"
    servers:
      hosts: 
        jmeter-server:
          ansible_port: 22 
          ansible_host: "{{ vault_jmeter_server_ip }}"
