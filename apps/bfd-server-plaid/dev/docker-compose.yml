version: '3.7'

services:

  postgresql:
    image: postgres:9.6
    ports:
      - "127.0.0.1:5432:5432"
    restart: always
    environment:
      #POSTGRES_DB: bfd
      POSTGRES_USER: bfd_user
      POSTGRES_PASSWORD: InsecureLocalDev
