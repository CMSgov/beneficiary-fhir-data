# V87__rebuild_claims_fk_constraints
# 
# Manually determine whether or not to execute this migration in a transaction. This is useful
# for databases like PostgreSQL where we specify that we want the lock update to allow sharing.
executeInTransaction=false