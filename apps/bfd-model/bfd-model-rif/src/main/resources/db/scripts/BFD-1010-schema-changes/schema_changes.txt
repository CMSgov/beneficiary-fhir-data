CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC(3,0) CHANGE to smallint:     a_mo_cnt                                 smallint,                             -- "partAMonthsCount"
NUMERIC(3,0) CHANGE to smallint:     b_mo_cnt                                 smallint,                             -- "partBMonthsCount"
NUMERIC(3,0) CHANGE to smallint:     buyin_mo_cnt                             smallint,                             -- "stateBuyInCoverageCount"
NUMERIC(3,0) CHANGE to smallint:     hmo_mo_cnt                               smallint,                             -- "hmoCoverageCount"
NUMERIC(3,0) CHANGE to smallint:     rds_mo_cnt                               smallint,                             -- "monthsRetireeDrugSubsidyCoverage"
NUMERIC(3,0) CHANGE to smallint:     age                                      smallint,                             -- "ageOfBeneficiary"
NUMERIC(3,0) CHANGE to smallint:     dual_mo_cnt                              smallint,                             -- "monthsOfDualEligibility"
NUMERIC(3,0) CHANGE to smallint:     plan_cvrg_mo_cnt                         smallint,                             -- "partDMonthsCount"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
CHARACTER CHANGE to bigint:     bene_id                                  bigint,                    -- "beneficiaryId"
CHARACTER CHANGE to bigint:     parent_beneficiary                       bigint not null,          -- "parentBeneficiary"
NUMERIC CHANGE to smallint:     clm_line_num                             smallint not null,                         -- "lineNumber"
CHARACTER CHANGE to bigint:     parent_claim                             bigint not null,           -- "parentClaim"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_alowd_chrg_amt                      numeric(10,2) not null,                   -- "allowedChargeAmount"
NUMERIC CHANGE to smallint:     carr_line_ansthsa_unit_cnt               smallint not null,                         -- "anesthesiaUnitCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_ptb_ddctbl_amt                 numeric(10,2) not null,                   -- "beneficiaryPartBDeductAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_pmt_amt                        numeric(10,2) not null,                   -- "beneficiaryPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_coinsrnc_amt                        numeric(10,2) not null,                   -- "coinsuranceAmount"
NUMERIC CHANGE to smallint:     dmerc_line_mtus_cnt                      smallint not null,                         -- "mtusCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_prvdr_pmt_amt                   numeric(10,2) not null,                   -- "providerPaymentAmount"
NUMERIC CHANGE to smallint:     line_srvc_cnt                            smallint not null,                         -- "serviceCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_sbmtd_chrg_amt                      numeric(10,2) not null                    -- "submittedChargeAmount"
CHARACTER CHANGE to bigint:     clm_id                                   bigint not null,           -- "claimId"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_alowd_chrg_amt                      numeric(10,2) not null,                   -- "allowedChargeAmount"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_ptb_ddctbl_amt                 numeric(10,2) not null,                   -- "beneficiaryPartBDeductAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_pmt_amt                        numeric(10,2) not null,                   -- "beneficiaryPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_prvdr_pmt_amt                   numeric(10,2) not null,                   -- "providerPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_sbmtd_chrg_amt                      numeric(10,2) not null,                   -- "submittedChargeAmount"
NUMERIC CHANGE to smallint:     clm_line_num                             smallint not null,                         -- "lineNumber"
CHARACTER CHANGE to bigint:     parent_claim                             bigint not null,           -- "parentClaim"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_alowd_chrg_amt                      numeric(10,2) not null,                   -- "allowedChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_ptb_ddctbl_amt                 numeric(10,2) not null,                   -- "beneficiaryPartBDeductAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_pmt_amt                        numeric(10,2) not null,                   -- "beneficiaryPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_coinsrnc_amt                        numeric(10,2) not null,                   -- "coinsuranceAmount"
NUMERIC CHANGE to smallint:     dmerc_line_mtus_cnt                      smallint not null,                         -- "mtusCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_prmry_alowd_chrg_amt                numeric(10,2) not null,                   -- "primaryPayerAllowedChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_prvdr_pmt_amt                   numeric(10,2) not null,                   -- "providerPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_dme_prchs_price_amt                 numeric(10,2) not null,                   -- "purchasePriceAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     dmerc_line_scrn_svgs_amt                 numeric(10,2),                            -- "screenSavingsAmount"
NUMERIC CHANGE to smallint:     line_srvc_cnt                            smallint not null,                         -- "serviceCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_sbmtd_chrg_amt                      numeric(10,2) not null,                   -- "submittedChargeAmount"
CHARACTER CHANGE to bigint:     clm_id                                   bigint not null,           -- "claimId"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_alowd_chrg_amt                      numeric(10,2) not null,                   -- "allowedChargeAmount"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_ptb_ddctbl_amt                 numeric(10,2) not null,                   -- "beneficiaryPartBDeductAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_pmt_amt                        numeric(10,2) not null,                   -- "beneficiaryPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_prvdr_pmt_amt                   numeric(10,2) not null,                   -- "providerPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_sbmtd_chrg_amt                      numeric(10,2) not null,                   -- "submittedChargeAmount"
NUMERIC CHANGE to smallint:     clm_line_num                             smallint not null,                         -- "lineNumber"
CHARACTER CHANGE to bigint:     parent_claim                             bigint not null,           -- "parentClaim"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_ncvrd_chrg_amt                  numeric(10,2) not null,                   -- "nonCoveredChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_rate_amt                        numeric(10,2) not null,                   -- "rateAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
NUMERIC CHANGE to smallint:     rev_cntr_unit_cnt                        smallint not null                          -- "unitCount"
CHARACTER CHANGE to bigint:     clm_id                                   bigint not null,           -- "claimId"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
NUMERIC CHANGE to smallint:     clm_line_num                             smallint not null,                         -- "lineNumber"
CHARACTER CHANGE to bigint:     parent_claim                             bigint not null,           -- "parentClaim"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_bene_pmt_amt                    numeric(10,2) not null,                   -- "benficiaryPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_ncvrd_chrg_amt                  numeric(10,2),                            -- "nonCoveredChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_prvdr_pmt_amt                   numeric(10,2) not null,                   -- "providerPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_rate_amt                        numeric(10,2) not null,                   -- "rateAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
NUMERIC CHANGE to smallint:     rev_cntr_unit_cnt                        smallint not null                          -- "unitCount"
CHARACTER CHANGE to bigint:     clm_id                                   bigint not null,           -- "claimId"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC(2,0) CHANGE to smallint:     bene_hospc_prd_cnt                       smallint,                             -- "hospicePeriodCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
NUMERIC CHANGE to smallint:     clm_utlztn_day_cnt                       smallint not null,                         -- "utilizationDayCount"
NUMERIC CHANGE to smallint:     clm_line_num                             smallint not null,                         -- "lineNumber"
CHARACTER CHANGE to bigint:     parent_claim                             bigint not null,           -- "parentClaim"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_ncvrd_chrg_amt                  numeric(10,2) not null,                   -- "nonCoveredChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_rate_amt                        numeric(10,2) not null,                   -- "rateAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
NUMERIC CHANGE to smallint:     rev_cntr_unit_cnt                        smallint not null                          -- "unitCount"
CHARACTER CHANGE to bigint:     clm_id                                   bigint not null,           -- "claimId"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_bene_blood_ddctbl_lblty_am           numeric(10,2) not null,                   -- "bloodDeductibleLiabilityAmount"
NUMERIC CHANGE to smallint:     nch_blood_pnts_frnshd_qty                smallint not null,                         -- "bloodPintsFurnishedQty"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_dsprprtnt_shr_amt           numeric(10,2),                            -- "claimPPSCapitalDisproportionateShareAmt"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_excptn_amt                  numeric(10,2),                            -- "claimPPSCapitalExceptionAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_fsp_amt                     numeric(10,2),                            -- "claimPPSCapitalFSPAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_ime_amt                     numeric(10,2),                            -- "claimPPSCapitalIMEAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_outlier_amt                 numeric(10,2),                            -- "claimPPSCapitalOutlierAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_old_cptl_hld_hrmls_amt           numeric(10,2),                            -- "claimPPSOldCapitalHoldHarmlessAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_tot_pps_cptl_amt                     numeric(10,2),                            -- "claimTotalPPSCapitalAmount"
NUMERIC CHANGE to smallint:     bene_tot_coinsrnc_days_cnt               smallint not null,                         -- "coinsuranceDayCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_bene_ip_ddctbl_amt                   numeric(10,2) not null,                   -- "deductibleAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     dsh_op_clm_val_amt                       numeric(10,2),                            -- "disproportionateShareAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_drg_outlier_aprvd_pmt_amt            numeric(10,2),                            -- "drgOutlierApprovedPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     ime_op_clm_val_amt                       numeric(10,2),                            -- "indirectMedicalEducationAmount"
NUMERIC CHANGE to smallint:     clm_non_utlztn_days_cnt                  smallint not null,                         -- "nonUtilizationDayCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_ip_ncvrd_chrg_amt                    numeric(10,2) not null,                   -- "noncoveredCharge"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_bene_pta_coinsrnc_lblty_am           numeric(10,2) not null,                   -- "partACoinsuranceLiabilityAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pass_thru_per_diem_amt               numeric(10,2) not null,                   -- "passThruPerDiemAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_profnl_cmpnt_chrg_amt                numeric(10,2) not null,                   -- "professionalComponentCharge"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_ip_tot_ddctn_amt                     numeric(10,2) not null,                   -- "totalDeductionAmount"
NUMERIC CHANGE to smallint:     clm_utlztn_day_cnt                       smallint not null,                         -- "utilizationDayCount"
NUMERIC CHANGE to smallint:     bene_mbi_id                              smallint not null,                         -- "medicareBeneficiaryIdKey"
CHARACTER CHANGE to bigint:     bene_id                                  bigint,                    -- "beneficiaryId"
CHARACTER CHANGE to bigint:     bene_ident_cd                            character varying(2),                     -- "beneficiaryIdCode"
NUMERIC CHANGE to smallint:     bene_mbi_id                              smallint not null,                         -- "medicareBeneficiaryIdKey"
CHARACTER CHANGE to bigint:     bene_id                                  bigint,                    -- "beneficiaryId"
CHARACTER CHANGE to bigint:     bene_ident_cd                            character varying(2),                     -- "beneficiaryIdCode"
NUMERIC CHANGE to smallint:     clm_line_num                             smallint not null,                         -- "lineNumber"
CHARACTER CHANGE to bigint:     parent_claim                             bigint not null,           -- "parentClaim"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_bene_pmt_amt                    numeric(10,2) not null,                   -- "benficiaryPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_blood_ddctbl_amt                numeric(10,2) not null,                   -- "bloodDeductibleAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_cash_ddctbl_amt                 numeric(10,2) not null,                   -- "cashDeductibleAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_1st_msp_pd_amt                  numeric(10,2) not null,                   -- "firstMspPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_ncvrd_chrg_amt                  numeric(10,2) not null,                   -- "nonCoveredChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_ptnt_rspnsblty_pmt              numeric(10,2) not null,                   -- "patientResponsibilityAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_prvdr_pmt_amt                   numeric(10,2) not null,                   -- "providerPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_rate_amt                        numeric(10,2) not null,                   -- "rateAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_rdcd_coinsrnc_amt               numeric(10,2) not null,                   -- "reducedCoinsuranceAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_2nd_msp_pd_amt                  numeric(10,2) not null,                   -- "secondMspPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
NUMERIC CHANGE to smallint:     rev_cntr_unit_cnt                        smallint not null,                         -- "unitCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_coinsrnc_wge_adjstd_c           numeric(10,2) not null                    -- "wageAdjustedCoinsuranceAmount"
CHARACTER CHANGE to bigint:     clm_id                                   bigint not null,           -- "claimId"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_bene_pmt_amt                        numeric(10,2) not null,                   -- "beneficiaryPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_bene_blood_ddctbl_lblty_am           numeric(10,2) not null,                   -- "bloodDeductibleLiabilityAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     line_coinsrnc_amt                        numeric(10,2) not null,                   -- "coinsuranceAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_bene_ip_ddctbl_amt                   numeric(10,2) not null,                   -- "deductibleAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_profnl_cmpnt_chrg_amt                numeric(10,2) not null,                   -- "professionalComponentCharge"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_prvdr_pmt_amt                   numeric(10,2) not null,                   -- "providerPaymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC CHANGE to smallint:     days_suply_num                           smallint not null,                         -- "daysSupply"
NUMERIC CHANGE to smallint:     fill_num                                 smallint not null,                         -- "fillNumber"
NUMERIC CHANGE to smallint:     clm_line_num                             smallint not null,                         -- "lineNumber"
CHARACTER CHANGE to bigint:     parent_claim                             bigint not null,           -- "parentClaim"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_ncvrd_chrg_amt                  numeric(10,2) not null,                   -- "nonCoveredChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_rate_amt                        numeric(10,2) not null,                   -- "rateAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
CHARACTER CHANGE to bigint:     clm_id                                   bigint not null,           -- "claimId"
CHARACTER CHANGE to bigint:     bene_id                                  bigint not null,           -- "beneficiaryId"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_bene_blood_ddctbl_lblty_am           numeric(10,2) not null,                   -- "bloodDeductibleLiabilityAmount"
NUMERIC CHANGE to smallint:     nch_blood_pnts_frnshd_qty                smallint not null,                         -- "bloodPintsFurnishedQty"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_dsprprtnt_shr_amt           numeric(10,2),                            -- "claimPPSCapitalDisproportionateShareAmt"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_excptn_amt                  numeric(10,2),                            -- "claimPPSCapitalExceptionAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_fsp_amt                     numeric(10,2),                            -- "claimPPSCapitalFSPAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_ime_amt                     numeric(10,2),                            -- "claimPPSCapitalIMEAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_cptl_outlier_amt                 numeric(10,2),                            -- "claimPPSCapitalOutlierAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pps_old_cptl_hld_hrmls_amt           numeric(10,2),                            -- "claimPPSOldCapitalHoldHarmlessAmount"
NUMERIC CHANGE to smallint:     bene_tot_coinsrnc_days_cnt               smallint not null,                         -- "coinsuranceDayCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_bene_ip_ddctbl_amt                   numeric(10,2) not null,                   -- "deductibleAmount"
NUMERIC CHANGE to smallint:     clm_non_utlztn_days_cnt                  smallint not null,                         -- "nonUtilizationDayCount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_ip_ncvrd_chrg_amt                    numeric(10,2) not null,                   -- "noncoveredCharge"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_bene_pta_coinsrnc_lblty_am           numeric(10,2) not null,                   -- "partACoinsuranceLiabilityAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     clm_pmt_amt                              numeric(10,2) not null,                   -- "paymentAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_prmry_pyr_clm_pd_amt                 numeric(10,2) not null,                   -- "primaryPayerPaidAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     rev_cntr_tot_chrg_amt                    numeric(10,2) not null,                   -- "totalChargeAmount"
NUMERIC(12,2) CHANGE to numeric(10,2):     nch_ip_tot_ddctn_amt                     numeric(10,2) not null,                   -- "totalDeductionAmount"
NUMERIC CHANGE to smallint:     clm_utlztn_day_cnt                       smallint not null,                         -- "utilizationDayCount"
