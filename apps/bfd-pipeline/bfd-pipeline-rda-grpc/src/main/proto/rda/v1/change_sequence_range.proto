syntax = "proto3";

package gov.cms.mpsm.rda.v1;

option java_multiple_files = true;

// Upper and lower bounds (inclusive) for claim change sequence numbers.
message ClaimSequenceNumberRange {
  int64 lower = 1;

  int64 upper = 2;
}
