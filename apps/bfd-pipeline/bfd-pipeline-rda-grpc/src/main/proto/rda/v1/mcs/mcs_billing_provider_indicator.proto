syntax = "proto3";

package gov.cms.mpsm.rda.v1.mcs;

option java_multiple_files = true;

import "rda/v1/enum_options.proto";

enum McsBillingProviderIndicator {
  // ASCII G
  BILLING_PROVIDER_INDICATOR_GROUP = 0 [(string_value) = "G"];
}
