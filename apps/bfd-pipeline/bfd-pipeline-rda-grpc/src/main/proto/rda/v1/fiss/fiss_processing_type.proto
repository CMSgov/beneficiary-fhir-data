syntax = "proto3";

package gov.cms.mpsm.rda.v1.fiss;

option java_multiple_files = true;

import "rda/v1/enum_options.proto";

// Processing Type Enum that maps Processing Types to ASCII values
enum FissProcessingType {
  PROCESSING_TYPE_BATCH = 0   [(string_value) = "B"];

  PROCESSING_TYPE_MANUAL = 1  [(string_value) = "M"];

  PROCESSING_TYPE_OFFLINE = 2 [(string_value) = "O"];
}
