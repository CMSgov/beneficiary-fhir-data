# Setup

Install packages

```sh
uv sync
```

# Development

Initialize the database with test data

```sh
./run-db.sh
```

Run the app

```sh
uv run ./pipeline.py
```

Run tests

```sh
uv run pytest
```
