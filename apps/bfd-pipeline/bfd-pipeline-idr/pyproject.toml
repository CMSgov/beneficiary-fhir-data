[project]
name = "bfd-pipeline-idr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psycopg>=3.2.9",
    "pydantic>=2.11.5",
    "snowflake-connector-python>=3.15.0",
]

[dependency-groups]
dev = ["pyright>=1.1.401", "pytest>=8.3.5", "testcontainers>=4.9.2"]

[[tool.mypy.overrides]]
module = ["testcontainers.*"]
follow_untyped_imports = true
