
repos:
  - repo: local
    hooks:
      - id: run-uv-pyright
        name: UV Run Pyright
        stages: [ pre-push ]
        entry: uv run pyright
        language: system
        verbose: true
        pass_filenames: false
