[tool.poetry]
package-mode = false
name = "convert-load-converter"

[tool.poetry.dependencies]
python = "^3.12"
boto3 = "^1.35.93"
types-boto3 = { extras = ["essential"], version = "^1.35.93" }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
