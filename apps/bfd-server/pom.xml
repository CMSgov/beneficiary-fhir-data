<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>gov.cms.bfd</groupId>
        <artifactId>bfd-parent</artifactId>
        <version>2.170.0-SNAPSHOT</version>
    </parent>

    <artifactId>bfd-server-parent</artifactId>
    <packaging>pom</packaging>

    <description>
        A parent POM for the bfd-server-* projects, which provide a FHIR-compliant API for querying Medicare beneficiary
        data.
    </description>

    <properties>
        <!-- Configure the BFD Server, as it will be run via the exec
            plugin. These settings are pulled out as POM properties so that they can
            be adjusted via profiles. -->
        <its.bfdServer.jvmargs>-Xmx4g</its.bfdServer.jvmargs>

        <!-- Should work out of the box on Linux, but likely needs to be customized
            (in settings.xml) for Windows dev environments. Seedev/devenv-readme.md
            for details. -->
        <bash.exe>bash</bash.exe>
    </properties>

    <modules>
        <module>bfd-server-docs</module>
        <module>bfd-server-launcher-sample</module>
        <module>bfd-server-launcher</module>
        <module>bfd-server-shared-utils</module>
        <module>bfd-server-war</module>
        <module>bfd-server-image</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <!-- Provides the JPA entities used to represent the Blue Button API
                    backend's data in the database. -->
                <groupId>gov.cms.bfd</groupId>
                <artifactId>bfd-model-rif</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <!-- Provides the JPA entities used to represent the Blue Button API
                    backend's data in the database. -->
                <groupId>gov.cms.bfd</groupId>
                <artifactId>bfd-model-rda</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>javax.servlet</groupId>
                <artifactId>javax.servlet-api</artifactId>
                <version>4.0.1</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <!-- Provides sample data used in tests. -->
                <groupId>gov.cms.bfd</groupId>
                <artifactId>bfd-model-rif-samples</artifactId>
                <version>${project.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <!-- Used to load sample data in tests. -->
                <groupId>gov.cms.bfd</groupId>
                <artifactId>bfd-pipeline-rif-load</artifactId>
                <version>${project.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>gov.cms.bfd.data.fda.utility</groupId>
                <artifactId>bfd-data-fda</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <!-- Provides utility code and dependencies that are shared by (more or
                less) all of the other BFD modules. -->
            <groupId>gov.cms.bfd</groupId>
            <artifactId>bfd-shared-utils</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>gov.cms.bfd.data.npi</groupId>
            <artifactId>bfd-data-npi</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-war-plugin</artifactId>
                    <configuration>
                        <attachClasses>true</attachClasses>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <!-- BFD-3767 -->
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.0.0</version>
                <executions>
                    <execution>
                        <?m2e execute onConfiguration,onIncremental?>
                        <id>download-nr</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>/usr/bin/curl</executable>
                            <arguments>
                                <argument>-o</argument>
                                <argument>${project.build.directory}/newrelic-java.zip</argument>
                                <argument>https://download.newrelic.com/newrelic/java-agent/newrelic-agent/current/newrelic-java.zip </argument>
                            </arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>unzip-file</id>
                        <phase>package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>unzip</executable>
                            <arguments>
                                <argument>${project.build.directory}/newrelic-java.zip</argument>
                                <argument>-d</argument>
                                <argument>${project.build.directory}/nr-java</argument>
                            </arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>template-server-script</id>
                        <phase>package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>${project.build.directory}/bfd-server-launcher/src/main/assembly/bin/fill-template.sh</executable>
                            <arguments>
                                <argument>${project.build.directory}/bfd-server-launcher/src/main/assembly/bin/bfd-server.sh.tmpl</argument>
                                <argument>${project.build.directory}/bfd-server-launcher/src/main/assembly/bin/bfd-server.sh</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!-- Used to build docker image for the project. -->
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-maven-plugin</artifactId>
                <configuration>
                    <to>
                        <image>${jib.namespace}/bfd-server:${project.version}</image>
                        <tags>
                            <tag>${jib.tag}</tag>
                        </tags>
                    </to>
                    <from>
                        <image>${jib.from}</image>
                        <platforms>
                            <platform>
                                <architecture>amd64</architecture>
                                <os>linux</os>
                            </platform>
                        </platforms>
                    </from>
                    <container>
                        <!-- <mainClass>gov.cms.bfd.server.launcher.DataServerLauncherApp</mainClass> -->
                        <!--  <ports><port>6500</port></ports>  -->
                        <appRoot>/usr/local/bfd-server</appRoot>
                        <environment>
                            <BFD_VERSION>${project.version}</BFD_VERSION>
                        </environment>
                        <entrypoint>/usr/local/bfd-server/bin/bfd-server.sh</entrypoint>
                        <jvmFlags>-Dorg.jboss.logging.provider=slf4j</jvmFlags>
                        <ports>
                            <port>7443</port>
                        </ports>
                        <workingDirectory>/usr/local/bfd-server</workingDirectory>
                    </container>
                    <extraDirectories>
                        <paths>
                            <!-- Copies war file into /app directory in container -->
                            <path>
                                <from>${project.build.directory}/war-file</from>
                                <!--  <into>/app</into>  -->
                                <into>/usr/local/bfd-server</into>
                            </path>
                            <!-- Copies Launcher artifacts into /app directory in container -->
                            <path>
                                <from>${project.build.directory}/server-launcher</from>
                                <!--  <into>/app</into>  -->
                                <into>/usr/local/bfd-server/server-launcher</into>
                            </path>
                            <path>
                                <from>${project.build.directory}/nr-java</from>
                                <into>/usr/local/bfd-server/newrelic</into>
                            </path>
                            <path>
                                <from>${project.build.directory}/bfd-server-launcher/src/main/assembly/bin</from>
                                <into>/usr/local/bfd-server/bin</into>
                            </path>
                        </paths>
                        <permissions>
                            <permission>
                                <file>/usr/local/bfd-server/bin/*.sh</file>
                                <mode>755</mode>
                            </permission>
                        </permissions>
                    </extraDirectories>
                </configuration>
                <executions>
                    <execution>
                        <id>build-image</id>
                        <phase>package</phase>
                        <goals>
                            <goal>${jib.goal}</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
