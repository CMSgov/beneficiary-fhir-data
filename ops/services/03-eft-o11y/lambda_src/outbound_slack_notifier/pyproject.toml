[project]
name = "outbound-slack-notifier"
version = "1.0.0"
dependencies = ["aws-lambda-powertools[all]"]

[dependency-groups]
dev = ["pyright", "ruff"]

# Inherits all tool configurations from 
# ../../../../pyproject.toml (ops intermediate)

