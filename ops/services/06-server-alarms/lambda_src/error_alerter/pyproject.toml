[project]
name = "error-alerter"
version = "1.0.0"
readme = "README.md"
dependencies = ["boto3"]

[dependency-groups]
dev = ["boto3-stubs[logs]", "ruff", "uv"]

# Inherits all tool configurations from 
# ../../../../pyproject.toml (ops intermediate)

[tool.pyright]
venvPath = "."
venv = ".venv"

