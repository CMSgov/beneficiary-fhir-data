[project]
name = "regression-wrapper"
version = "1.0.0"
dependencies = ["aws-lambda-powertools[all]>=3.8.0", "boto3>=1.37.14"]

[dependency-groups]
dev = ["boto3-stubs[lambda,codedeploy]", "ruff", "uv"]

# Inherits all tool configurations from 
# ../../../../pyproject.toml (ops intermediate)
