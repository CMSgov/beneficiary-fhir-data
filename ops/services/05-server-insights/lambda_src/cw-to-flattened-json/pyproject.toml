[project]
name = "cw-to-flattened-json"
version = "1.0.0"
dependencies = ["boto3"]

[dependency-groups]
dev = ["ruff", "uv"]

# Inherits all tool configurations from 
# ../../../../pyproject.toml (ops intermediate)

[tool.pyright]
venvPath = "."
venv = ".venv"


