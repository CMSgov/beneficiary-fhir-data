---

- name: Test ssh_users role
  hosts: localhost
  connection: local
  gather_facts: no
  vars:
    ansible_ssh_pipelining: no

  tasks:
    - include_vars:
        file: vars/000_cross_env_vars.yml
    - import_role:
        name: ssh_users
