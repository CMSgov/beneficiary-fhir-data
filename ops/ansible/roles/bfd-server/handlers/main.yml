---

##
# Note: With these imports, Ansible will act as though all the imported tasks were just listed in
# this file, instead. They still have to have name or listen properties that match the topics being
# notified.
##

- import_tasks: enable_appserver.yml
- import_tasks: restart_appserver.yml
