#!/usr/bin/env groovy

properties([
        parameters([
                booleanParam(name: 'test_parameter', description: 'This is a test parameter'),
                string(name: 'test_param_string', description: 'Test parameter string type')
        ])
])

try {
    node('POD_LABEL') {
        stage('CallMain') {
            script {
                def mainJenkinsFile = load('./Jenkinsfile')
                mainJenkinsFile.method()
            }
        }
    }
} finally {

}