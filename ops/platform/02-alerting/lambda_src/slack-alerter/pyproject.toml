[project]
name = "slack-alerter"
version = "1.0.0"
dependencies = ["aws-lambda-powertools[all]", "boto3", "pydantic"]

[dependency-groups]
dev = ["ruff", "uv"]

# Inherits all tool configurations from 
# ../../../../pyproject.toml (ops intermediate)
