apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: postgres
  ports:
    - port: 5432
      protocol: TCP
      name: "port-5432"
