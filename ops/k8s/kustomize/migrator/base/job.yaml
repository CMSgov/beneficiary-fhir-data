apiVersion: batch/v1
kind: Job
metadata:
  name: migrator
spec:
  template:
    spec:
      containers:
      - name: migrator
        image: bfd-db-migrator:1.0.0-SNAPSHOT
      restartPolicy: Never
  backoffLimit: 4
